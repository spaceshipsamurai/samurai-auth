div.row
   div.col-md-6.col-lg-6.col-sm-12
      table.table.table-hover.table-striped
         thead
            tr
               th Name
               th Location
               th Harvest Date
         tbody
            tr(ng-repeat="recruit in recruits")
               td {{ recruit.name }}
               td {{ recruit.system }}
               td {{ recruit.createdDate | date : 'dd MMMM yyyy HH:mm:ss'}}
            tr(ng-show="!recruits || recruits.length == 0")
               td(colspan=3) No names have been harvested. Please <a ui-sref="recruitment.add">add</a> names to continue.